## Backend
backend/__pycache__/
backend/.venv/
*.pyc

## Security review (Aug 2026) — a stray dev-server log (backend/server.log)
## and a legacy backup file with a hardcoded default secret
## (backend/main.py.phase2.bak) were both previously committed by accident.
*.log
*.bak

## Frontend
frontend/node_modules/
frontend/dist/
frontend/dev-dist/

## Secrets — never commit real credentials (Phase 3 / Supabase)
## (.env.example files are intentionally NOT ignored — they're templates)
.env
.env.local
.env.production
.env.development
backend/.env
backend/.env.local
backend/.env.production
backend/.env.development
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.development
*.pem
*.key

## OS / editor
.DS_Store
.vscode/

## Claude Code project memory — local only, not for the repo
CLAUDE.md

## Generated graph diagnostic reports — rewritten by validate_walkway_graph.py
## and route_quality_test.py on every run; noisy multi-thousand-line diffs,
## no source data, safe to regenerate locally whenever needed.
backend/data/validation_report.json
backend/data/route_quality_report.json
